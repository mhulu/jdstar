export const loading = ({ commit }) => commit('loading')
export const loaded = ({ commit }) => commit('loaded')
export const userInfo = ({ commit }) => commit('userInfo')
export const profile = ({ commit }) => commit('profile')
export const weather = ({ commit }) => commit('weather')
// export const notifications = ({ commit }) => commit('notifications')